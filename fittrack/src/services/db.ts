// Local persistence placeholder.
// On web, the SQLite (expo-sqlite) approach from the original plan does not apply.
// Real client-side caching will use IndexedDB (or rely on the backend) in a later phase.

export async function initDb(): Promise<void> {
  // Implemented in a later phase
}
